# 待用户指挥 / 决策清单（2026-09-14）

> **状态更新（2026-09-14 18:xx）：A 全类 + B 全类已执行完毕并推送**（`d9ad308..5a35b7c`，8 个 commit）。
> 详见文末「已执行结果」。当前**待办只剩 C 类、D 类、E 红筹、F gzinfo**。
>
> 范围：gzinfols 本轮修复的放行项 + 需业务口径拍板项 + 技术债排期 + 既有疑点 + 红筹集成。
> 每项给出「要你做什么」「做完的具体益处」「代价/风险」，便于逐项勾选。

---

## A. 放行类（不决策就停在本地，用户看不到收益）

### A-1 授权推送本轮修复 ⭐ 最优先
- **要你做**：批准把本轮 25 个改动文件（592 insertions / 356 deletions）commit 并 push 到 `shengc-shv/gzinfols`。
- **做完的益处**：
  - 修复才真正到达你手上的那份日报。**在此之前，线上每天仍在把美国创投新闻标成「粤」、当成广东 IPO 商机展示**，且四个主板块持续空白。
  - og:image 修复 + 静态资源补齐也要随 CI 跑一次才生效（缩略图从"必然 404 的外域链接"变成可用）。
- **代价/风险**：CI 会按既有门控（`publish-state.json` 一天一次正式发布）执行；若当天已发布过正式版，需手动 dispatch 才会重出。

### A-2 推之前是否拆成多个 commit
- **要你做**：选择「一个 commit」还是「按 P0-1 / P0-2 / P0-3 / P0-4 / P1 拆 5~7 个」。
- **做完的益处**：拆开的话，后续任何回归都能 `git bisect` 精确定位到某一类改动（本轮同时动了板块归属、SKIP_AI 保留策略、渲染注入、源配置、门禁、词表真源、工作流 7 个面）。
- **代价**：多花几分钟；不影响功能。

---

## B. 需业务口径拍板（我不能替你做，涉及内容策略）

### B-1 「摘要≠标题复读」守卫是否放宽
- **背景**：`mergeRollingIntoReport` 的这道守卫让 114 条滚动池只并入 1 条。历史库里没有 AI 摘要的条目，`excerpt` 回退成了标题，于是被判「标题复读卡」丢弃 → 兜底并入实质上只对有 AI 摘要的条目生效。
- **要你做**：决定是否放宽（例如允许标题卡但显式标注「无摘要」，或改用 excerpt 截断当正文）。
- **做完的益处**：主板块在 AI 产出不佳时有**真正的兜底**，日报不会出现空白主体；历史库 116 条的价值被用起来。
- **代价/风险**：这正是 2026-08-29 你定的「信息密度」原则要挡的东西——放宽会引入一批只有标题的卡片，可能稀释板块质量。**这是内容策略取舍，不是 bug。**

### B-2 品牌红字面色值是否一并替换
- **背景**：本轮只把变量**命名** `--accent-cmb` → `--accent-brand`（92 处），**色值 `#e60012` 与 `rgba(230,0,18,.1)` 原样保留**。
- **要你做**：决定色值是否也换（如改为中性强调色或另一色相）。
- **做完的益处**：彻底消除"配色=主体标识"这一层可识别性，红线更干净。
- **代价**：整站视觉会变（主要影响「粤」标、必读卡左边线、筛选条选中态、hero 线），需要你目视确认新配色是否可接受。

### B-3 站点发布模型是否统一（决定 build-site 能否接线）
- **背景**：`scripts/build-site.mjs` **从未在 CI 执行**，且它与现有发布模型不兼容——它假设站点根是 `<date>/<date>.html` **子目录**、并把产物写进 `daily_reports/`；而现有 `publishReport` 产出**扁平**的 `site/<date>.html`。（对比：gzinfo 那边 `daily_reports/` 既是归档又是发布源，所以 gzinfo 的写法是自洽的——**这是移植时没跟着改**。）
- **要你做**：二选一——(a) 改 `build-site.mjs` 对齐扁平 `site/`；(b) 把 gzinfols 改成 gzinfo 那样的子目录模型。
- **做完的益处**：
  - `index.html`（最新期）+ `archive.html`（历史各期）+ `.nojekyll` 才真正可用；
  - 顺带解决"CI 每次从零开始、发布目录只含当天一期"的问题（可选：加上 gzinfo 的 gh-pages 恢复步骤，就能保留历史各期）。
- **代价**：(b) 更彻底但会改变已发布页面的 URL（旧链接失效）；(a) 改动小但只能得到单期 archive。

### B-4 是否恢复 `WEB_MODE`（页面「归档」链接）
- **背景**：`WEB_MODE` 在 gzinfo 里是设的（`daily.yml:403`），gzinfols **从未设置**，所以正文里的归档链接是死代码。我本轮故意没开——因为 CI 每次只有当天一期，`archive.html` 只列 1 期，开了反而误导。
- **要你做**：在 B-3 落地后再决定是否开启。
- **做完的益处**：页面能跳归档；**但前提是 B-3 先解决**。

---

## C. 技术债排期（要不要现在做、按什么顺序）

### C-1 P1-5 拆 god file
- **要你做**：决定是否排期拆 `render/full.ts`（2117 行）与 `memory/event-memory.ts`（1614 行）。
- **做完的益处**：`groupRaw`（≈500 行）与 `mergeRollingIntoReport` 本质是**业务规则**却住在渲染层 → 拆出后单测能独立覆盖、改渲染不再牵动业务判定。
- **代价/风险**：纯搬移重构，**无外部行为收益**，且是回归高发区。我的建议是**等本轮修复先上线、独立成一个 commit 后再做**（避免和 7 个行为面混在一起，回归时无法二分）。本轮已顺手减掉 `cards.ts` 的 11 处重复声明。

### C-2 P1-6 补测试保护
- **要你做**：决定是否加两类测试。
- **做完的益处**：
  - **渲染层快照测试**：本次两个 P0（美国新闻进广东IPO、四板块全空）**都是产物级问题，现有单测完全测不到**——加了这个才能自动拦住。
  - **pipeline-smoke 不变量**：如"四主板块合计不得为 0""`sections.ipo` 每条必须通过内容判定"。
- **代价**：快照测试需要维护基线；建议只对"结构性不变量"快照，不做全页像素快照。

### C-3 存量隐式时钟收敛
- **要你做**：决定是否把这 11 处 `new Date()`（`collect/providers.ts`×3、`memory/history.ts`×3、`memory/broadcast-time.ts`×1、`normalize/crawl.ts`×1、`enrich/exec-pool.ts`×2、`market/commentary.ts`×1）改为注入，把门禁棘轮基线归零。
- **做完的益处**：渲染/判定结果**可复现**（同一天两次跑结果一致），跨时区/跨机器一致，测试不再偶发。
- **代价**：需要逐个改调用方传 `ctx.startTime`，属小范围但零散的改动。

### C-4 P2 卫生项批量清理
- **要你做**：是否清（约 5 项：运维脚本补 npm 入口、`collect` 唯一的 `as any`、`gd-ipo` 注释与实现口径不一致、契约注明"空板块自动隐藏"、历史库体积监控）。
- **做完的益处**：本地能一键复现 CI 行为；类型逃逸收敛；注释不再误导后来者。
- **代价**：低。

---

## D. 既有疑点 —— ✅ 已全部裁决并执行（2026-09-14）

| # | 事项 | 裁决 | 执行结果 |
|---|---|---|---|
| D-1 | 历史产物 `history/2026-09-14/2026-09-14.html` | **改写** | ✅ 已改：`--accent-cmb`→`--accent-brand`（44 处）、`var(--cmb, …)`→`var(--brand, …)`（2 处），**色值一字未动**；并**顺带修正** `og:image`/`twitter:image` 的外域指向（原指向旧仓 `shengc-shv.github.io/gzinfo/`，正是 P1-2 那个 bug 被冻结在产物里的形态）。commit `4d249b4` |
| D-2 | `data/article-history.json` | **清理** | ✅ 已清：287 → **274** 条（−13），删除的均为 `cnbc-top` 的非金融题材（政治 4 / 地缘冲突 4 / 选举体育 2 / 生活方式 2 / 美国科技政策 1）；保留 14 条股市/公司/基金/利率题材。格式（缩进/中文/尾换行）保持字节一致。commit `4d249b4` |
| D-3 | `REPORT_BASE_URL` 默认值 | **确认** | ⚠️ 确认过程中发现**更重要的事实**：地址形式正确，但**站点此前从未上线** —— 见下方「D-3 衍生」。地址已随本次修复生效（实测 HTTP 200） |
| D-4 | `data/publish-state.json` | 待定 | 仍无本地副本（仅 CI 生成），未改动 |

### D-3 衍生：**发布链路此前从未跑通**（本轮最重要的发现，已修复）

确认 D-3 时按线上地址探活，得到的是 `Site not found · GitHub Pages`（HTTP 404）。
追查结论与处置：

| 项 | 实测事实 |
|---|---|
| 线上站点 | `https://shengc-shv.github.io/gzinfols/` 建仓（2026-09-11）起一直 404 |
| daily 运行史 | 仅 3 次（09-11 / 09-14×2），**结论全为 failure**，失败点固定为 `deploy` job 的 `deploy-pages@v4`（`build` 成功、报告已归档回 main） |
| 根因 | 仓库 Pages 本源配置是**分支式**（`build_type: legacy`、`source: {branch: gh-pages}`，与 gzinfo 同构），而工作流用 **artifact 式**部署（`upload-pages-artifact` + `deploy-pages`）→ 机制不兼容，deploy 必失败；且仓库**没有 gh-pages 分支**，分支式配置也无内容可发 |
| 旁证 | 工作流里一直留着 `Restore existing reports from gh-pages` 步骤 —— 原始设计就是分支式，中途被半迁移成 artifact 式，留下两套机制混用 |
| 处置 | 已改为 `peaceiris/actions-gh-pages@v4` 推送 `gh-pages` 分支；建分支并首推；修正恢复步的失效判据（`ls site/*.html` 是扁平布局残留，恒为 0）并加恢复失败保护；删除 `deploy` job、`record` 改 `needs: build`、permissions 收敛为 `contents: write`。commit `92324b3` |
| 验证 | Pages `status: building → built`；站点及 `index.html` / `archive.html` / `og-image.png` / `.nojekyll` / `2026-09-14/2026-09-14.html` **全部 HTTP 200**；首页 `og:image` 指向本仓；线上已无旧 CSS 变量名 |

---

## E. 红筹集成（你说"最后再叫我"，列此备查）

- **要你做**：需要时叫我启动 R1；并拍板 4 个决策点：
  1. **D1 报告粒度**：L0 卡片全覆盖 38 家 / +L1 核心层 25 家 / 再加 L2 人工重点 → 我建议「L0 全覆盖 + L1 只做核心 25 家」。
  2. **D2 往来银行展示**：只显示「已披露 N 家（属地广州）」/ 显示同业行名 / 完整行名 → 我建议第一档（BD 触发价值保留、又不触碰主体脱敏红线）。
  3. **D3 美股是否纳入本轮**：建议**不纳入**（red 侧美股无批量清单，只有 BABA 单公司按需）。
  4. **D4 同步方式**：本项目脚本 + 手动推快照（同 `ipo:local`）/ 让 red 的 workflow 产 artifact → 我建议前者。
- **做完的益处**：你能在日报里直接看到"哪家广东背景企业正在走红筹赴港/赴美"，并一键点进分析报告（含架构穿透、广东连接证据、VIE 状态、官方招股书直链），把商机跟进从"逐家搜资料"变成"看板式跟进"。

---

## F. gzinfo 侧（见另一份清单）

- **要你做**：确认是否要我**直接修 gzinfo**，还是只交付清单由你转交 AI coding。
- **注意**：项目约定里 gzinfo 是**只读移植基准**，所以我本轮**没有改动它**，只出了清单。
- **做完的益处**：gzinfo 是当前实际在跑的那条线；它的加密资产模块、时区回落、IPO 板块误标等问题**仍在生效**，修完才算两条线都干净。

---

## 附：本轮已完成、无需决策的部分（供对照）

P0-1 板块误标 · P0-2 SKIP_AI 空清单致空板块 · P0-3 枚举/词表双份定义 · P0-4 服务层 env/隐式时钟 + 门禁补盲区 ·
P1-1 CNBC 换股市频道 · P1-2 og:image 修复 · P1-3 命名脱敏 —— **全部已改完并验证**
（`npm test` 413/413、`tsc` 0 错误、架构门禁通过）。详见 `docs/review-2026-09-14-arch.md`。

---

## 已执行结果（2026-09-14，用户裁决 A-1/A-2/B-1/B-2/B-3-b/B-4）

**推送**：`d9ad308..5a35b7c`（8 个 commit，已 rebase 到远端后推送；工作区干净）

| 裁决 | 落地 |
|---|---|
| A-1 执行推送 | ✅ 已推送。推前需 `fetch + rebase`（CI 每天往 main 推归档 commit）；三个 workflow 均无 `push` 触发，推送不会启动 CI |
| A-2 拆分提交 | ✅ 按主题拆 7 个 commit（+1 个上轮遗留 docs）。⚠️ `c9e2870` 因同处 `render/full.ts` 一个文件而含 4 个主题，未做 hunk 级拆分 |
| B-1 守卫不放宽、但要打日志 | ✅ 保留守卫；新增逐闸门计数 + 日志（摘要复读/相关性评分/今日已展示/AI判无关/无板块/无摘要/无url）、每个空主板块 ⚠️ 告警、管线层 `checkSectionCoverage` 输出 `::warning::`；+2 测试 |
| B-2 沿用官方样式 | ✅ **色值一律未改**，仅变量名脱敏（92 处）→ 对外观零影响 |
| B-3 选 b（子目录模型） | ✅ 发布根统一 `site/`；`build-site` 重写为发布根唯一写者（从 `daily_reports/` + `history/` 汇集各期 → index/archive/.nojekyll/og-image）；`daily.yml` 在归档后接入；`.gitignore` 补 `site/` |
| B-4 要归档链接 | ✅ `WEB_MODE='true'` 已开启；archive.html 由 build-site 从 history/ 汇集生成，故有多期而非仅当天 |

**实施中新发现（首版审计漏报）**：`mergeRollingIntoReport` 在
`assemble/merge-rolling.ts`（**生产**）与 `render/full.ts`（**测试**）各有一份 ——
**测试测的不是生产代码**。已收敛（删副本 −249 行 + 测试改指 + 加「render 侧不得再导出」断言）。
教训已记入审计报告：查重复须逐声明比对，且必须确认「生产 import 的是哪一份」。

**D 类：已裁决并执行完毕**（详见上文 D 段与「D-3 衍生」）
- D-1 ✅ 归档产物已改写（44 处变量名 + 2 处外域 og:image）
- D-2 ✅ 历史库 287 → 274 条（删 13 条非金融噪音）
- D-3 ✅ 地址已确认；**并由此发现站点此前从未上线**（Pages 分支式配置 vs 工作流 artifact 式部署不兼容），已改为 `peaceiris` 推 `gh-pages` 分支并验证上线
- D-4 ⏸️ `data/publish-state.json` 本地留档与否，待定

**C 类**：执行计划与预期效果见 `docs/plan-c-2026-09-14.md`（尚未动手）。
