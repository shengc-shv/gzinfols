# 2026-09-11 gzinfols 2.0 全面回检 + 回检优化落地（软件团队协作）

## 下午：按回检报告落实 P0/P1/P2 优化（用户拍板：CI 用 DeepSeek 账号跑）

### 团队执行（TeamCreate: software-gzinfols，已完成并删除）
- 工程师（寇豆码）：14 项全清单实现，commit **d5a982c**（25 files, +1028/-260）
- QA（严过关）第 1 轮：IS_PASS NO，抓到 F1 真实 Bug（skip-ai 泄漏 LLM）+ 7/7 红线新用例
- 工程师修复 F1/F3，commit **eff1498**（skip-ai 零 LLM 断言 tests/skip-ai.test.ts）
- QA 第 2 轮：**IS_PASS YES**，独立实证修复正确，35/35 测试全绿，三红线无回退

### 本轮落地的核心改造
- P0-B2：报告级 LLM 喂真实条目（每板块前 8 条 title+url+summary），must_read.url 校验 ∈ 今日集合
- P0-B1：新增 lib/services/enrich/relevance.ts AI 相关性回检（参考区豁免、商机/风险命中跳过、30 条/批、仅显式 relevant=false 才丢弃）
- P1：LLM 批量化（20 条/批 + 4 并发池），调用次数 = ⌈N/20⌉+⌈M/30⌉+1；ctx.stats 观测；历史库闭环（dedupeAgainstHistory + 真 30 天裁剪 + rollingSince 修复）；窗口过滤注入 ctx.startTime；env 收敛 ctx.config
- P1-T6：llm.ts viaDeepSeek（DEEPSEEK_BASE_URL/MODEL/API_KEY），CI LLM_BACKEND=deepseek，cron 改工作日 6:30/12:00/17:30 CST（30 22 * * 0-4 / 0 4 * * 1-5 / 30 9 * * 1-5 UTC）
- P2：SECTION_ORDER/SECTION_LABELS 收敛契约层；SECTION_KEYWORD_GROUPS 词表常量；RunMode 裁剪死字段；CrawlerRegistry 上移契约+接入 PipelineDeps.crawlers；ReportItem.published_at ISO；C1 纯透传/加工收敛 C2；补 select/assemble/memory/render 测试；新 AGENTS.md

### 行为变化（已确认接受）
1. assignSection 删除 category==="tech" 增补关键词（红线#2 精确化），纯 tech 词表外条目落 policy_market
2. 兜底必读优先级统一跟随渲染 tab 顺序（SECTION_ORDER）
3. 相关性回检宁误放策略（仅显式 false 丢弃）

### 遗留（不阻塞）
- F2（P3）：llm.ts Backend 类型含 minimax 无实现分支，后续收窄或补实现
- T7（P3）：fetchScrape per-site 选择器迁移（gzinfo crawlers 目录）未做；CrawlerRegistry 仅接通道未装配实现
- CI 跑通需在 GitHub repo secrets 配 DEEPSEEK_API_KEY；**两次 commit 均未 push**（等用户授权）

### 关键评审结论（待用户拍板，未动代码）
- 补齐 2.0 脚手架收尾：CI（.github/workflows/daily.yml）写入成功；node_modules 安装（此前缺失导致 tsx/tsc 不可用）。
- 全面回检（25 lib + 5 scripts + 6 tests + CI），报告落盘 `docs/review-2026-09-11.md`。
- 回检中发现并修复 7 组问题，基线归零：tsc 0 错误 · 架构门禁通过 · 12/12 测试通过。

## 本次修复清单（已改文件）
- `lib/services/assemble/index.ts`：`T1.5` 键加引号（TS1005）；改从 contracts/report 导入报告类型。
- `lib/contracts/pipeline.ts`：AssembledResult 补 ReportMustRead/ReportInsight/RiskItem 导入；IngestResult.articles 改为诚实的 `RawArticle[]`、删 `rawArticles`；游离 import 上移。
- `lib/services/collect/index.ts`：删除未使用的 rawArticles 字段返回。
- `lib/contracts/article.ts`：RawArticle 增可选 `isIpo?`（collect 附加、normalize 收敛为必填）。
- `lib/services/memory/index.ts`：HistoryItem 显式类型（含 source/publishedAt）；Object.entries 断言消隐式 any。
- `tests/normalize.test.ts`：ctx 注入 SilentLog；`tests/pipeline.e2e.test.ts`：readJson 补 await。

## 关键评审结论（待用户拍板，未动代码）
- P0-B1：红线#3 被架空——funnel 只做 L0 硬排除，注释声称"AI 回检"但 enrich 无回检，弱相关条目会混入 policy_market 兜底。
- P0-B2：enrich 报告级 LLM 调用只喂板块条数，hero/insights/must_read 无真实条目支撑，must_read.url 必幻觉死链。
- P1：历史库只写不读（跨天重复无去重）；rollingSince 因 MM/DD 格式实际失效；LLM 逐条串行调用（成本爆炸）；AI 失败零观测；Clock 端口存在但 select 用 Date.now()。
- P2：板块标签 ×3 重复、TIER_WEIGHT 与 SOURCE_TIER_ORDER 重复；skip-ai 死字段；CrawlerRegistry 未接主链路；CI 默认 claude-cli 在 Actions 必然静默降级。
